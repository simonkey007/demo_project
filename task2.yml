---
    platform: linux
    image_resource:
        type: docker-image
        source:
            repository: amazon/aws-cli
    run:
        path: sts
        args:
        - assume-role
        - --role-arn
        - arn:aws:iam::617007418524:role/automation-concourse
        - --role-session-name
        - s3-access-example