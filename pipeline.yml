resources:
    - name: src
      type: git
      source:
        uri: git@github.com:simonkey007/demo_project.git
        branch: master
        private_key: ((github-private-key))

jobs:
    - name: test credentials
      plan:
      - get: src
        trigger: true
      - task: echo
        vars:
            szymon: ((test))
        file: src/task.yml
      - task: sts
        file: src/task2.yml
    